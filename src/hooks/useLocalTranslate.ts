import { LocaleType } from '@peter-present/react-hook-utils';
import en from 'src/locale/en.json';
import vi from 'src/locale/vi.json';

const _locale = { en, vi } as LocaleType;

export default function useLocalTranslate() {
  //
}
